function init() {

    // Color the correct page on navBar
    let $navBar = $('#nav4');
    $navBar.css({
        'color' : 'var(--pink)'
    });
}


$('document').ready(function () {
    init();
});

$(window).scroll(function () {

});