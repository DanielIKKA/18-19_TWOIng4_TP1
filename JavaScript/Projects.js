function init() {

    // Color the correct page on navBar
    let $navBar = $('#nav3');
    $navBar.css({
        'color' : 'var(--pink)'
    });
}


$('document').ready(function () {
    init();
});

$(window).scroll(function () {

});